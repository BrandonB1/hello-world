#include <stdio.h> // includes the functions to do things like print to the screen, //take user input, etc.



int main(int argc, const char * argv[]) // the function main is where a C program begins and ends, and it is of type int, meaining it returns an integer. The parenthesis enclose command line arguments, and within the curly braces, is the code.
{
    printf("Hello World!"); // prints the arguments to the screen, what is in quotation marks
    return 0; // ends the program
}
